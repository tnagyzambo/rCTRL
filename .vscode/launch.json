{
    "configurations": [
        {
            "name": "Remote launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/rdata/rdata_lc_example", // Local path.
            "initCommands": [
                "platform select remote-linux",
                "platform connect connect://localhost:3000",
                "settings set target.inherit-env true", // See the note below.
            ],
            // "env": {
            //     "PATH": "...", // See the note below.
            // }
        }
    ]
}